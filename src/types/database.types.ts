// このファイルは自動生成されます
// 型定義を生成するには以下のコマンドを実行してください：
// npm run db:types

export type Database = {
  public: {
    Tables: {
      // テーブル定義がここに自動生成されます
    }
    Views: {
      // ビュー定義がここに自動生成されます
    }
    Functions: {
      // 関数定義がここに自動生成されます
    }
    Enums: {
      // 列挙型定義がここに自動生成されます
    }
    CompositeTypes: {
      // 複合型定義がここに自動生成されます
    }
  }
}
